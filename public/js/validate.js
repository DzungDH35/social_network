function isNameValid(name) {
    let validNameFormat = /^[a-zA-ZÀÁÂÃÈÉÊÌÍÒÓÔÕÙÚĂĐĨŨƠàáâãèéêìíòóôõùúăđĩũơƯĂẠẢẤẦẨẪẬẮẰẲẴẶẸẺẼỀỀỂẾưăạảấầẩẫậắằẳẵặẹẻẽềềểếỄỆỈỊỌỎỐỒỔỖỘỚỜỞỠỢỤỦỨỪễệỉịọỏốồổỗộớờởỡợụủứừỬỮỰỲỴÝỶỸửữựỳỵỷỹ]+(?: [a-zA-ZÀÁÂÃÈÉÊÌÍÒÓÔÕÙÚĂĐĨŨƠàáâãèéêìíòóôõùúăđĩũơƯĂẠẢẤẦẨẪẬẮẰẲẴẶẸẺẼỀỀỂẾưăạảấầẩẫậắằẳẵặẹẻẽềềểếỄỆỈỊỌỎỐỒỔỖỘỚỜỞỠỢỤỦỨỪễệỉịọỏốồổỗộớờởỡợụủứừỬỮỰỲỴÝỶỸửữựỳỵỷỹ]+)*$/;
    return validNameFormat.test(name);
}

function isUserNameValid(username) {
    let validUsernameFormat = /^[A-Za-z0-9]+(?:[ _-][A-Za-z0-9]+)*$/;
    return validUsernameFormat.test(username);
}

function isEmailValid(email) {
    let validEmailFormat = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/;
    return validEmailFormat.test(email);
}

function isPasswordValid(password) {
    let validPassFormat = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&_])[A-Za-z\d@$!%*?&_]{8,20}$/;
    return validPassFormat.test(password);
}

function isPasswordConfirmed(password, cfpassword) { return cfpassword === password; }